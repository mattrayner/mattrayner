%nav.navbar.navbar-inverse.navbar-fixed-top
  .container
    .navbar-header
      %button.navbar-toggle.collapsed{ type: 'button', data: { toggle: 'collapse', target: '#navbar' }, aria: { expanded: 'false', controls: 'navbar' } }
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{ href: admin_dashboard_index_path }
        Matt Rayner Admin
        %small
          %span.glyphicon.glyphicon-home
    #navbar.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li
          %a{ href: root_path }
            Live Site
        %li{ class: if(params[:controller] == 'admin/case_studies') then 'active' end }
          %a{ href: admin_case_studies_path }
            Case Studies
      %ul.nav.navbar-nav.navbar-right
        %li.dropdown
          %a.dropdown-toggle{ href: '#', data: { toggle: 'dropdown', role: 'button' }, aria: { expanded: 'false' } }
            = image_tag current_user.gravatar_url, class: 'img-circle'
            %span.caret
          %ul.dropdown-menu.inverse-dropdown{ role: 'menu' }
            %li
              = link_to 'Edit Profile', edit_user_registration_path, :class => 'navbar-link'
            %li.divider
            %li
              = link_to 'Log Out', destroy_user_session_path, method: :delete, :class => 'navbar-link'